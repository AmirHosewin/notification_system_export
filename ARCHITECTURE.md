# System Architecture

Visual overview of the notification system architecture.

## 📊 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          TTLock Smart Lock System                            │
│                         (FastAPI Application)                                 │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │
        ┌─────────────────────────────┴──────────────────────────────┐
        │                                                              │
        │                  Notification System                         │
        │                  (app/notification_system/)                  │
        │                                                              │
        │  ┌────────────────────────────────────────────────────┐   │
        │  │           API Layer (FastAPI Routes)                │   │
        │  │                                                      │   │
        │  │  • POST /v1/register_fcm_token                     │   │
        │  │  • GET  /v1/notifications                          │   │
        │  │  • PUT  /v1/notifications/{id}/read                │   │
        │  │  • PUT  /v1/notifications/read_all                 │   │
        │  │  • GET  /v1/notifications/stats                    │   │
        │  │  • POST /v1/test_notification (DEBUG)              │   │
        │  └────────────────────────────────────────────────────┘   │
        │                          ↓                                  │
        │  ┌────────────────────────────────────────────────────┐   │
        │  │         Service Layer (Business Logic)              │   │
        │  │                                                      │   │
        │  │  ┌──────────────────┐    ┌──────────────────┐     │   │
        │  │  │ NotificationSvc  │◄───│  FCM Service     │     │   │
        │  │  │                  │    │                  │     │   │
        │  │  │ • Create & Send  │    │ • Send FCM      │     │   │
        │  │  │ • Get History    │    │ • Batch Send    │     │   │
        │  │  │ • Mark as Read   │    │ • Validate Token│     │   │
        │  │  │ • Statistics     │    │ • Log Delivery  │     │   │
        │  │  └──────────────────┘    └──────────────────┘     │   │
        │  │           ↓                        ↓                │   │
        │  │  ┌──────────────────┐    ┌──────────────────┐     │   │
        │  │  │ Message Builder  │    │ Firebase Admin   │     │   │
        │  │  │                  │    │      SDK         │     │   │
        │  │  │ • 9 Types        │    │                  │     │   │
        │  │  │ • Priority       │    │ • messaging.send│     │   │
        │  │  │ • Data Payload   │    │ • Token Check   │     │   │
        │  │  └──────────────────┘    └──────────────────┘     │   │
        │  └────────────────────────────────────────────────────┘   │
        │                          ↓                                  │
        │  ┌────────────────────────────────────────────────────┐   │
        │  │          Integration Hooks                          │   │
        │  │                                                      │   │
        │  │  • check_and_notify_low_battery()                  │   │
        │  │  • notify_device_unlock()                          │   │
        │  │  • notify_gateway_offline()                        │   │
        │  └────────────────────────────────────────────────────┘   │
        │                          ↓                                  │
        │  ┌────────────────────────────────────────────────────┐   │
        │  │         Database Layer (SQLAlchemy)                 │   │
        │  │                                                      │   │
        │  │  • notification_table                              │   │
        │  │  • notification_log_table                          │   │
        │  │  • battery_alert_tracker_table                     │   │
        │  │  • user_table (fcm_token fields)                   │   │
        │  └────────────────────────────────────────────────────┘   │
        │                                                              │
        └──────────────────────────────────────────────────────────────┘
                          ↓                          ↓
                          │                          │
        ┌─────────────────┴────────┐    ┌────────────┴──────────────┐
        │  Firebase Cloud Platform  │    │   PostgreSQL Database     │
        │                           │    │                           │
        │  • FCM Service            │    │  • notification_table     │
        │  • Token Management       │    │  • notification_log_table │
        │  • Push Delivery          │    │  • battery_alert_tracker  │
        └───────────────────────────┘    └───────────────────────────┘
                          ↓
        ┌────────────────────────────────────────────┐
        │          Android Devices                    │
        │                                             │
        │  • FCM Client SDK                          │
        │  • Notification Handler                    │
        │  • Token Registration                      │
        └────────────────────────────────────────────┘
```

## 🔄 Data Flow: Low Battery Alert

```
1. Device Battery Record Created
   ↓
2. records.py POST /v1/add_record
   • Create Record in database
   • Extract battery_level from record_info.electric_quantity
   ↓
3. Hook Triggered: check_and_notify_low_battery()
   • Check battery_level ≤ threshold (20%)
   • Check cooldown period (24h)
   • Check battery drop ≥ 5%
   ↓
4. NotificationService.create_and_send_notification()
   • Get user with FCM token
   • Build notification message
   • Create notification record in DB
   ↓
5. MessageBuilder.build_notification()
   • Type: LOW_BATTERY
   • Priority: HIGH
   • Title: "⚠️ Low Battery Alert"
   • Body: "Device X battery is at Y%..."
   • Data payload with device info
   ↓
6. FCMService.send_push_notification()
   • Build FCM message with Android config
   • Send via Firebase Admin SDK
   • Log delivery attempt
   ↓
7. Firebase Cloud Messaging
   • Validate FCM token
   • Queue for delivery
   • Push to device
   ↓
8. Android Device
   • Receive notification
   • Display in notification tray
   • Handle tap → navigate to device screen
   ↓
9. Database Updates
   • notification_table: status = "sent"
   • notification_log_table: log success/failure
   • battery_alert_tracker_table: update cooldown
```

## 📦 Component Relationships

```
┌──────────────────────────────────────────────────────────┐
│                   notification_system/                    │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  DB/                    Services/                         │
│  ├─ notification_models.py   ├─ notification_service.py  │
│  ├─ notification_schemas.py  ├─ fcm_service.py           │
│  └─ migration.sql            └─ notification_messages.py  │
│         ↓                            ↓                    │
│         └────────────┬───────────────┘                    │
│                      ↓                                    │
│                 hooks/                                    │
│                 └─ device_hooks.py                        │
│                      ↓                                    │
│                 api/                                      │
│                 └─ notification_routes.py                 │
│                      ↓                                    │
│                 utils/                                    │
│                 └─ notification_types.py                  │
│                                                           │
│  FB/                                                      │
│  └─ firebase_config.py                                   │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

## 🗄️ Database Schema

```
┌──────────────────────────────────────────────────────────┐
│                      user_table                           │
├──────────────────────────────────────────────────────────┤
│ id                    INTEGER PRIMARY KEY                 │
│ email                 VARCHAR                             │
│ ...                   (existing fields)                   │
│ fcm_token             VARCHAR(255)  ← NEW                │
│ fcm_token_updated_at  TIMESTAMP     ← NEW                │
└──────────────────────────────────────────────────────────┘
                         ↓ 1:N
┌──────────────────────────────────────────────────────────┐
│                  notification_table                       │
├──────────────────────────────────────────────────────────┤
│ id                    BIGSERIAL PRIMARY KEY               │
│ user_id               INTEGER FK → user_table             │
│ notification_type     VARCHAR(50)                         │
│ priority              VARCHAR(10) [high|normal]           │
│ title                 VARCHAR(255)                        │
│ body                  TEXT                                │
│ data                  JSONB                               │
│ device_id             INTEGER FK → device_table           │
│ gateway_id            INTEGER FK → gateway_table          │
│ ekey_id               INTEGER FK → ekey_table             │
│ metadata              JSONB                               │
│ status                VARCHAR(20) [pending|sent|failed|read] │
│ created_at            TIMESTAMP DEFAULT NOW()             │
│ sent_at               TIMESTAMP                           │
│ read_at               TIMESTAMP                           │
└──────────────────────────────────────────────────────────┘
                         ↓ 1:N
┌──────────────────────────────────────────────────────────┐
│                notification_log_table                     │
├──────────────────────────────────────────────────────────┤
│ id                    BIGSERIAL PRIMARY KEY               │
│ notification_id       BIGINT FK → notification_table      │
│ attempt_number        INTEGER DEFAULT 1                   │
│ fcm_response          TEXT                                │
│ status                VARCHAR(20) [success|failed]        │
│ error_message         TEXT                                │
│ created_at            TIMESTAMP DEFAULT NOW()             │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│            battery_alert_tracker_table                    │
├──────────────────────────────────────────────────────────┤
│ id                        BIGSERIAL PRIMARY KEY           │
│ device_id                 INTEGER UNIQUE FK → device_table│
│ last_alert_at             TIMESTAMP                       │
│ battery_level_at_alert    INTEGER                         │
│ alert_count               INTEGER DEFAULT 1               │
│ created_at                TIMESTAMP DEFAULT NOW()         │
│ updated_at                TIMESTAMP DEFAULT NOW()         │
└──────────────────────────────────────────────────────────┘
```

## 🔐 Security Architecture

```
┌────────────────────────────────────────────────────────┐
│                   Android App                           │
└────────────────────────────────────────────────────────┘
                         ↓ HTTPS
┌────────────────────────────────────────────────────────┐
│                  API Gateway / Nginx                    │
│  • SSL/TLS Termination                                 │
│  • Rate Limiting                                       │
└────────────────────────────────────────────────────────┘
                         ↓
┌────────────────────────────────────────────────────────┐
│              FastAPI Application                        │
│                                                         │
│  1. JWT Authentication                                 │
│     └─ get_current_user()                             │
│                                                         │
│  2. User Authorization                                 │
│     └─ Verify token ownership                         │
│                                                         │
│  3. Input Validation                                   │
│     └─ Pydantic schemas                               │
│                                                         │
│  4. Database Security                                  │
│     └─ SQLAlchemy ORM (SQL injection prevention)      │
│                                                         │
│  5. Firebase Admin SDK                                 │
│     └─ Service account credentials                    │
│                                                         │
└────────────────────────────────────────────────────────┘
                    ↓              ↓
        ┌───────────┴─────┐  ┌────┴──────────┐
        │   PostgreSQL     │  │   Firebase    │
        │   Database       │  │   Platform    │
        │                  │  │               │
        │  • Encrypted     │  │  • OAuth2     │
        │  • Firewall      │  │  • API Keys   │
        └──────────────────┘  └───────────────┘
```

## ⚡ Performance Characteristics

```
┌─────────────────────────────────────────────────────────┐
│           Async/Await Architecture                       │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  FastAPI Request                                        │
│       ↓ (non-blocking)                                  │
│  Database Query (async)                                 │
│       ↓ (non-blocking)                                  │
│  Firebase SDK (asyncio.to_thread)                       │
│       ↓ (non-blocking)                                  │
│  Response                                               │
│                                                          │
│  Multiple requests handled concurrently                 │
│                                                          │
│  Connection Pool:                                       │
│  • 20 base connections                                  │
│  • 30 overflow connections                              │
│  • Automatic cleanup                                    │
│                                                          │
│  Performance Metrics:                                   │
│  • API Response: < 100ms (local DB)                    │
│  • FCM Send: ~200-500ms (Firebase latency)             │
│  • Database Query: < 50ms (with indexes)               │
│  • Concurrent Users: 100+ (limited by DB pool)         │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

## 🔄 Notification Type Priority Matrix

```
┌───────────────────────────────────────────────────────┐
│          Notification Type → Priority                  │
├───────────────────────────────────────────────────────┤
│                                                        │
│  LOW_BATTERY          →  HIGH   ⚠️  (Immediate)      │
│  ═══════════════════════════════════════════════════  │
│  DEVICE_UNLOCK        →  NORMAL 🔓                    │
│  DEVICE_LOCK          →  NORMAL 🔒                    │
│  EKEY_SHARED          →  NORMAL 🔑                    │
│  EKEY_REVOKED         →  NORMAL 🔑                    │
│  GATEWAY_OFFLINE      →  NORMAL 📡                    │
│  GATEWAY_ONLINE       →  NORMAL 📡                    │
│  SECURITY_ALERT       →  NORMAL 🚨                    │
│  NEW_DEVICE_LOGIN     →  NORMAL 🔐                    │
│                                                        │
│  Priority Levels:                                     │
│  • HIGH: Bypass battery optimization, show immediately│
│  • NORMAL: Standard delivery, respect user settings   │
│                                                        │
└───────────────────────────────────────────────────────┘
```

## 📊 Integration Points

```
┌──────────────────────────────────────────────────────────┐
│             Existing TTLock Application                   │
└──────────────────────────────────────────────────────────┘
                         │
        ┌────────────────┼────────────────┐
        ↓                ↓                ↓
┌───────────────┐ ┌──────────────┐ ┌────────────────┐
│   records.py  │ │   main.py    │ │  settings.py   │
├───────────────┤ ├──────────────┤ ├────────────────┤
│ • Line ~77-90 │ │ • lifespan() │ │ • 11 new vars  │
│ • Hook call   │ │ • Router reg │ │ • FCM config   │
│ • Try-except  │ │ • Firebase   │ │ • Thresholds   │
│ • Non-block   │ │   init       │ │ • Defaults     │
└───────────────┘ └──────────────┘ └────────────────┘
        ↓                ↓                ↓
        └────────────────┼────────────────┘
                         ↓
        ┌────────────────────────────────┐
        │    Notification System         │
        │    (Standalone Module)         │
        └────────────────────────────────┘
```

## 🎯 Deployment Architecture

```
┌─────────────────────────────────────────────────────────┐
│                   Production Server                      │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌────────────────────────────────────────────────┐   │
│  │              Nginx (Reverse Proxy)              │   │
│  │  • SSL/TLS                                      │   │
│  │  • Rate Limiting                                │   │
│  │  • Load Balancing                               │   │
│  └────────────────────────────────────────────────┘   │
│                         ↓                               │
│  ┌────────────────────────────────────────────────┐   │
│  │         FastAPI Application (Uvicorn)           │   │
│  │                                                  │   │
│  │  app/                                           │   │
│  │  ├── notification_system/  ← Notification Sys  │   │
│  │  ├── api/                                       │   │
│  │  ├── services/                                  │   │
│  │  └── ...                                        │   │
│  │                                                  │   │
│  │  Environment:                                   │   │
│  │  • main.env (FCM config)                       │   │
│  │  • Firebase credentials                         │   │
│  └────────────────────────────────────────────────┘   │
│                         ↓                               │
│  ┌────────────────────────────────────────────────┐   │
│  │           PostgreSQL Database                   │   │
│  │  • notification_table                          │   │
│  │  • notification_log_table                      │   │
│  │  • battery_alert_tracker_table                 │   │
│  └────────────────────────────────────────────────┘   │
│                                                          │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│              External Services (Cloud)                   │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Firebase Cloud Platform                                │
│  • Project: ttlock-notifications                        │
│  • Service: Cloud Messaging (FCM)                       │
│  • Authentication: Service Account                      │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

## 🔧 Configuration Flow

```
┌──────────────┐
│   main.env   │  (Environment Variables)
└──────────────┘
       ↓
┌──────────────────────────┐
│   app/config/settings.py │
│                          │
│  • NOTIFICATION_ENABLED  │
│  • NOTIFICATION_FCM_*    │
│  • LOW_BATTERY_*         │
│  • DEBUG                 │
└──────────────────────────┘
       ↓
┌───────────────────────────────────┐
│  Notification System Components   │
│                                   │
│  • firebase_config.py             │
│  • notification_service.py        │
│  • fcm_service.py                 │
│  • device_hooks.py                │
│  • notification_routes.py         │
└───────────────────────────────────┘
```

## 📈 Monitoring & Observability

```
┌────────────────────────────────────────────────────────┐
│                  Monitoring Layers                      │
├────────────────────────────────────────────────────────┤
│                                                         │
│  1. Application Logs                                   │
│     • ✅ Success: "Firebase initialized..."          │
│     • ⚠️ Warning: "User has no FCM token..."         │
│     • ❌ Error: "FCM send failed..."                 │
│                                                         │
│  2. Database Monitoring                                │
│     • notification_table (all records)                │
│     • notification_log_table (delivery tracking)      │
│     • Query performance (EXPLAIN ANALYZE)             │
│                                                         │
│  3. Firebase Console                                   │
│     • Message delivery stats                          │
│     • Token validity                                  │
│     • Error rates                                     │
│                                                         │
│  4. Application Metrics                                │
│     • API response times                              │
│     • Notification throughput                         │
│     • Success/failure rates                           │
│     • User engagement (read rates)                    │
│                                                         │
└────────────────────────────────────────────────────────┘
```

---

## 📚 Architecture Principles

### 1. **Modularity**
- Standalone notification system
- Can be extracted to separate repository
- Minimal coupling with main application

### 2. **Scalability**
- Async/await for high concurrency
- Connection pooling for efficiency
- Batch operations support
- Horizontal scaling ready

### 3. **Reliability**
- Comprehensive error handling
- Delivery tracking and logging
- Retry mechanisms (via FCM)
- Non-blocking integration

### 4. **Security**
- JWT authentication required
- User authorization checks
- Input validation (Pydantic)
- Secure credential storage

### 5. **Maintainability**
- Clear separation of concerns
- Well-documented code
- Type hints throughout
- Comprehensive test coverage potential

### 6. **Performance**
- Database indexes optimized
- Async operations throughout
- Minimal blocking operations
- Efficient resource usage

---

**Architecture Version:** 1.0.0
**Last Updated:** January 15, 2025
